import { PropertyUpdateEventConsumer } from './property-update-event-consumer';

describe('PropertyUpdateEventConsumer', () => {
  it('should create an instance', () => {
    expect(new PropertyUpdateEventConsumer()).toBeTruthy();
  });
});
